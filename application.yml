
server:
  port: 8099
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8772/eureka/
    fetch-registry: true
    register-with-eureka: true
  instance:
    prefer-ip-address: true
